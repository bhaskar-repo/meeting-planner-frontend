import { FirstCharDirective } from './first-char.directive';

describe('FirstCharDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstCharDirective();
    expect(directive).toBeTruthy();
  });
});
